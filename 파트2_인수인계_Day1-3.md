# 파트2 (로직/백엔드) 인수인계 문서 — Day1~3

작성일 기준: Day3 (7/27) 완료 시점
담당: 센아 (파트2 — 스케줄/위험감지 로직)

---

## 1. 완료된 작업 요약

| Day | 내용 | 상태 |
|---|---|---|
| Day1 | 프로젝트 구조 설계, 스케줄 JSON 포맷 설계, 위험감지 로직 목업 | ✅ 완료 |
| Day2 | `/scan` 토픽 구조 학습(Gazebo 실측), 위험감지 함수 단위 테스트 | ✅ 완료 |
| Day3 | 스케줄 매니저 노드 초안 (rclpy + nav2_simple_commander) | ✅ 완료 (빌드 성공, 실행 테스트는 미완) |

---

## 2. 전달하는 파일 목록 및 배치 위치

```
patrol_robot/
├── config/
│   └── schedule.json              ← 여기에 배치
├── patrol_robot/
│   ├── risk_detector_node.py      ← 이미 배치됨 (Day4에서 완성 예정)
│   └── schedule_manager_node.py   ← 여기에 배치 (오늘 작성)
```

- `risk_logic.py`(순수 로직, ROS2 의존성 없음)는 아직 패키지에 편입 안 함 — Day4에서 `risk_detector_node.py`가 이 로직을 흡수할 예정이라 별도 유틸 모듈로 뺄지, 노드에 합칠지는 Day4 때 같이 결정하면 좋을 것 같음.

---

## 3. schedule.json 포맷 설명

```json
{
  "zones": [
    {"zone_id": "zone_a", "zone_name": "생산라인 A", "waypoints": [{"waypoint_id": "a_1", "x": 1.2, "y": 0.5, "yaw": 0.0}]}
  ],
  "time_blocks": [
    {"block_id": "operation", "start_time": "09:00", "end_time": "12:00", "patrol_order": ["zone_a", "zone_b"], "base_priority": 3}
  ]
}
```

- **`zones`**: 실제 좌표(웨이포인트) 정의. `x`, `y`는 map 좌표계 기준, `yaw`는 라디안.
- **`time_blocks`**: 시간대별 순찰 순서(`patrol_order`)와 `base_priority`(나중에 Day6 동적 재조정에서 위험 가중치와 합산 예정).

**중요 — 팀원 확인 필요한 부분:**
현재 `zone_a`, `zone_b`의 `x`/`y` 좌표는 **임의로 넣은 예시값**입니다. 파트1에서 만드는 실제 맵(map.yaml)의 좌표계와 안 맞을 가능성이 높아요. **Day5 병합 전에 실제 맵 기준으로 좌표를 다시 잡아야 합니다.**

---

## 4. risk_logic.py — 위험감지 순수 로직

ROS2 의존성 없는 순수 함수라 팀원도 바로 테스트 가능합니다 (`python3 risk_logic.py`로 단독 실행하면 테스트 케이스 5개 자동 실행됨).

- `detect_risk(ranges, angle_min, angle_increment, threshold, sensor_range_min, sensor_range_max)` — 핵심 함수
- **`sensor_range_min`/`sensor_range_max` 꼭 넘겨주세요**: `/scan` 메시지의 `range_min`(0.12m)/`range_max`(3.5m, 실측값)를 그대로 안 넘기면 라이더 자기 몸체 반사 등으로 오탐 발생 가능.
- 임계값(`threshold`) 기본값 0.5m로 설정해뒀는데, 실제 시연 환경 기준으로 재조정 필요할 수 있음.

---

## 5. schedule_manager_node.py — 스케줄 매니저 (초안)

**하는 일**: 현재 시각 → 해당 time_block 찾기 → `patrol_order` 순서대로 Nav2에 goal 순차 전송

**아직 안 된 것 (알려진 한계, Day4~6에서 채울 예정):**
1. `night` 블록처럼 `end_time`이 자정을 넘는 경우(`18:00~08:00`) 시간 매칭 로직이 아직 처리 못 함
2. 지금은 한 바퀴만 순회(`run_once`) — 무한 반복 순찰 로직 없음
3. 위험감지 이벤트에 따른 goal 취소/재전송(동적 재조정)은 Day6 몫, 아직 미구현

**의존성 (package.xml에 추가함, 파트1 쪽 환경에도 필요):**
- `nav2_simple_commander`
- `tf_transformations`
- `geometry_msgs`

**빌드는 성공했으나 실행 테스트는 아직 안 함** — Nav2 + map이 준비된 환경에서만 실제로 goal 전송이 되는지 확인 가능하기 때문에, 파트1 쪽 Nav2 세팅 완료되면 같이 테스트 필요.

---

## 6. 환경 세팅 관련 트러블슈팅 공유 (파트1 쪽에도 유용할 내용)

Day1~2에 겪은 이슈들인데, 팀원 환경에서도 똑같이 발생할 수 있어서 미리 공유합니다.

### 6-1. Gazebo Classic vs 신형 Gazebo(gz-harmonic) 충돌
- 증상: `apt install ros-humble-turtlebot3-simulations` 시 `gz-tools2` 관련 의존성 충돌
- 원인: 시스템에 신형 Gazebo(Harmonic, gz-sim8 계열)가 이미 설치돼 있으면 TurtleBot3(Classic 필요)와 공존 불가
- 해결: `gz-*`, `libgz-*`, `libignition-*` 패키지 전부 purge 후, `ros-humble-desktop`, `nav2`, `rviz2`, `slam_toolbox`, `turtlebot3` 관련 패키지를 **한 번에** 재설치 (순서 중요 — 따로따로 하면 또 충돌 남)

### 6-2. `/spawn_entity` 서비스 unavailable 에러
- 증상: `ros2 launch turtlebot3_gazebo empty_world.launch.py` 실행 시 로봇이 world엔 안 뜨고 spawn 실패
- 원인: `GAZEBO_PLUGIN_PATH` 환경변수에 ROS2용 플러그인 경로(`/opt/ros/humble/lib`)가 빠져 있어서 `libgazebo_ros_factory.so`를 못 찾음
- 해결:
  ```bash
  echo 'export GAZEBO_PLUGIN_PATH=/opt/ros/humble/lib:$GAZEBO_PLUGIN_PATH' >> ~/.bashrc
  source ~/.bashrc
  ```

### 6-3. RViz Nav2 플러그인 로딩 실패 (`libdiagnostic_updater.so` undefined symbol)
- 증상: Nav2 실행 시 RViz는 뜨지만 Navigation2 패널이 "could not be loaded" 에러
- 원인: 6-1에서 재설치하는 과정에서 `diagnostic_updater` 관련 패키지 버전이 꼬였을 가능성 (원인 조사 중, 미해결 — 파트1 쪽에서 이어서 확인 필요)
- 임시 대응: `dpkg -l | grep diagnostic`으로 버전 확인 후 `--reinstall` 시도 예정

---

## 7. Day5 병합 체크포인트 전까지 파트1에 요청할 것

1. 실제 사용할 맵(map.yaml) 완성되면 좌표계 공유 → `schedule.json`의 `x`/`y` 값 재조정
2. RViz Nav2 플러그인 에러(6-3) 해결 여부 공유
3. Nav2 스택이 정상 기동되면 `schedule_manager_node.py` 같이 실행 테스트

---

*이 문서는 Day3 완료 시점 기준이며, Day4 이후 진행 상황은 별도 업데이트 예정.*
